# Comma-separated list of origins allowed to call this API (your React frontend URL(s))
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.onrender.com

# Set automatically by Render — override locally if needed
PORT=8000

# Optional gunicorn tuning
WEB_CONCURRENCY=4
GUNICORN_TIMEOUT=120
LOG_LEVEL=info
